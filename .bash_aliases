# 
# ~/.bash-aliases
#

# Aliases that should only be applied in shell shells
# Aliases that should persist regardless of shell type (shell, zsh, etc) should be in ~/.aliases
# Aliases in this file will override aliases in ~/.aliases
