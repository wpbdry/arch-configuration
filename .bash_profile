#
# ~/.bash_profile
#

# bashrc - bash specific configurations
[[ -f ~/.bashrc ]] && . ~/.bashrc

# profile - stuff that applies to all shells.
# This line should be included in all shell specific profile files.
. ~/.profile

# bash_aliases - bash specific aliases
. ~/.bash_aliases
